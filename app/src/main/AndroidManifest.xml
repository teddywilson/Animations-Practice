<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="teddywilson.animationspractice" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >

        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".FadeActivity" />
        <activity android:name=".DotsActivity" />
        <activity android:name=".ScaleActivity" />
        <activity android:name=".ViewPagerActivity" />
        <activity android:name=".ExpandingFABActivity" />
        <activity android:name=".PinkActivity" />
        <activity android:name=".SharedImgActivityA" />
        <activity android:name=".SharedImgActivityB" />
        <activity android:name=".GestureActivity" />

    </application>

</manifest>